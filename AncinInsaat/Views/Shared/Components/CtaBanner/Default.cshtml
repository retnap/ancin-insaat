@model CtaBannerViewModel

<section class="cta-banner">
    <a class="cta-banner-link" href="@Model.LinkUrl">
        <div class="container cta-banner-inner">
            <span class="cta-banner-heading">@Model.Heading</span>
        </div>
    </a>
</section>
