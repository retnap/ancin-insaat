@model MissionVisionViewModel

@*
    About Us "Mission & Vision" image (2026-08-20 revision — project owner
    asked for the image area to contain only the image, with no text
    overlay; a follow-up request the same day removed the Misyon/Vizyon
    copy that had been sitting below it too, so the photo is now the
    section's entire content). MissionLabel/MissionText/VisionLabel/
    VisionText stay on the ViewModel/ViewComponent unused rather than
    removed, in case the client asks for this copy to reappear elsewhere.
*@

<section class="section mission-vision" id="about-mission-vision" aria-label="Misyon ve Vizyon">
    <div class="container">
        <img class="mission-vision-image" src="@Model.BackgroundImageUrl" alt="" loading="lazy" decoding="async" />
    </div>
</section>
